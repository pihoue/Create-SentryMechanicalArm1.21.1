pluginManagement {
    repositories {
        gradlePluginPortal()
        mavenLocal()
        maven {
            name = "NeoForged"
            url = uri("https://maven.neoforged.net/releases")
        }
    }
}
